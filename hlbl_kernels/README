This is a proxy environment created to test some optimised components of hadronic light by light (HLBL) code

To run the GPU code: make kernel
To run the CPU code: make test

This folder also contains a reference Makefile for compiling the HLbL code in folder makefile_hlbl
